# 🔗 Integration Tests - الاختبارات التكاملية

## 📋 المحتويات

- [نظرة عامة](#-نظرة-عامة)
- [الاختبارات المتوفرة](#-الاختبارات-المتوفرة)
- [الأدوات](#-الأدوات)

---

## 🌟 نظرة عامة

الاختبارات التكاملية تختبر APIs كاملة من البداية للنهاية.

---

## 📝 الاختبارات المتوفرة

### 1. Travel Packs Tests

```
tests/integration/travelPacks.test.ts
```

### 2. Activities Tests

```
tests/integration/activities.test.ts
```

### 3. Cars Tests

```
tests/integration/cars.test.ts
```

### 4. Pack Relations Tests

```
tests/integration/packRelation.integration.test.ts
```

### 5. Health Check Tests

```
tests/integration/health.test.ts
```

---

## 🛠️ الأدوات

### Supertest

```javascript
import request from 'supertest';
import app from '../../src/app';

const response = await request(app).get('/api/v1/travel-packs').expect(200);
```

### MongoDB Memory Server

```javascript
import { MongoMemoryServer } from 'mongodb-memory-server';

let mongoServer;
beforeAll(async () => {
  mongoServer = await MongoMemoryServer.create();
  await mongoose.connect(mongoServer.getUri());
});

afterAll(async () => {
  await mongoose.disconnect();
  await mongoServer.stop();
});
```

---

_📘 Auto-generated by Copilot Documentation Architect — ExploreKG Server Project_
