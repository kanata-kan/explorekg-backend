# 🧪 دليل الاختبارات - Testing Guide

## 📋 المحتويات

- [استراتيجية الاختبار](#-استراتيجية-الاختبار)
- [أنواع الاختبارات](#-أنواع-الاختبارات)
- [تشغيل الاختبارات](#-تشغيل-الاختبارات)

---

## 🎯 استراتيجية الاختبار

### الأهداف

✅ تغطية شاملة للكود  
✅ كشف الأخطاء مبكرًا  
✅ ضمان الجودة  
✅ توثيق السلوك

---

## 🔬 أنواع الاختبارات

### 1. Unit Tests

اختبار الوحدات الفردية (Services, Utils)

```javascript
describe('Guest Service', () => {
  it('should create guest with UUID', async () => {
    const guest = await guestService.createGuest({
      email: 'test@example.com',
      name: 'Test User',
    });
    expect(guest.sessionId).toMatch(
      /^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i
    );
  });
});
```

### 2. Integration Tests

اختبار APIs كاملة

```javascript
describe('POST /api/v1/guests', () => {
  it('should create guest and return 201', async () => {
    const res = await request(app)
      .post('/api/v1/guests')
      .send({ email: 'test@example.com', name: 'Test' });

    expect(res.status).toBe(201);
    expect(res.body.success).toBe(true);
  });
});
```

### 3. Quick Tests

اختبارات سريعة للتطوير

```bash
node --import tsx src/quick-test-guest.ts
node --import tsx src/quick-test-booking.ts
```

---

## ▶️ تشغيل الاختبارات

### Jest Tests

```bash
# All tests
pnpm test

# Watch mode
pnpm test:watch

# Coverage
pnpm test:coverage
```

### Quick Tests

```bash
# Guest system
node --import tsx src/quick-test-guest.ts

# Booking journey
node --import tsx src/quick-test-booking.ts
```

---

## 📊 Test Coverage

الهدف: **80%+ coverage**

```
Statements   : 85%
Branches     : 78%
Functions    : 82%
Lines        : 86%
```

---

_📘 Auto-generated by Copilot Documentation Architect — ExploreKG Server Project_
