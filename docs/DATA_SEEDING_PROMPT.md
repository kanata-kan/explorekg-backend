# 🌱 DATA SEEDING PROMPT

## 🎯 الهدف
تهيئة وإدخال البيانات (Activities, Cars, Travel Packs) باللغتين الإنجليزية والفرنسية في النظام،  
مع التحقق أولاً من تطابق الـschema مع بنية المشروع،  
باستخدام البيانات الموجودة داخل المجلد:
```
data/content/en
data/content/fr
```

---

## 🧠 تعليمات التنفيذ لـ Copilot

### 🪜 المرحلة 1 — مراجعة النماذج (Models Validation)
قبل أي إدخال للبيانات:
1. افتح ملفات النماذج داخل `src/models/`:
   - `activity.model.ts`
   - `car.model.ts`
   - `travelPack.model.ts`
2. راجع الـschema في كل ملف وتحقق من أسماء الحقول، أنواعها (types)، والـrequired fields.
3. **لا تقم بأي تعديل** بعد المراجعة — فقط قم بمقارنتها مع البيانات القادمة من المجلد:
   ```
   data/content/en
   data/content/fr
   ```

> ⚠️ الغرض من هذه الخطوة هو التأكد من أن البيانات التي سيتم إدخالها متوافقة 100% مع نماذج المشروع الحالي.

---

### 🪜 المرحلة 2 — مطابقة البيانات مع النماذج (Schema Alignment)
1. بعد المراجعة، عدّل **البيانات محليًا داخل الذاكرة فقط** لتتطابق مع الـschema.
   - أزل أي مفاتيح غير معرفة في النموذج.
   - أضف القيم الافتراضية (default values) إذا كان النموذج يحتاجها.
2. لا تكتب أي شيء في قاعدة البيانات بعد.

> ✅ الهدف: الحصول على نسخة جاهزة من البيانات مطابقة تماماً للنظام دون أي خطأ في الحقول.

---

### 🪜 المرحلة 3 — إدخال البيانات (Data Seeding)
بعد الموافقة الصريحة مني (عبدالإله):

1. أنشئ ملف Script جديد:
   ```bash
   src/scripts/seedContent.ts
   ```
2. قم بإدخال البيانات بالترتيب التالي:
   - Activities → `Activity` collection  
     📁 المصدر: `data/content/en/activities` و `data/content/fr/activities`
   - Cars → `Car` collection  
     📁 المصدر: `data/content/en/cars` و `data/content/fr/cars`
   - Travel Packs → `TravelPack` collection  
     📁 المصدر: `data/content/en/travel-packs` و `data/content/fr/travel-packs`

3. تأكد من إدخال كل زوج لغوي تحت نفس `localId` بالهيكل التالي:
   ```json
   {
     "localId": "activity-1",
     "translations": {
       "en": {...},
       "fr": {...}
     }
   }
   ```
4. بعد الإدخال، نفّذ اختبار تحقق:
   - عدد العناصر المدخلة في كل لغة متساوٍ.
   - لا يوجد أي حقل مفقود أو غير متوافق.
   - كل `metadata.path` يبدأ بـ `/activities/`, `/cars/`, أو `/travel-packs/` حسب الفئة.

---

### 🪜 المرحلة 4 — إنشاء تقرير التنفيذ (Data Seeding Report)
بعد انتهاء عملية الإدخال:

1. أنشئ ملف جديد:
   ```bash
   DATA_SEEDING_REPORT.md
   ```
2. تضمّن فيه:
   - 📦 عدد العناصر المدخلة لكل قسم.
   - ⏱️ مدة التنفيذ.
   - ✅ العناصر التي تم إدخالها بنجاح.
   - ⚠️ العناصر التي تم تجاهلها (إن وُجدت).
   - 🔍 مقارنة بين اللغتين (EN/FR consistency check).
3. أضف توقيع التنفيذ في الأسفل:
   ```text
   Executed by Copilot on DATE — Reviewed by Abdelaelah (Kanata)
   ```

---

## ⚙️ القواعد العامة

- لا تقم بتعديل أي كود داخل `src/models/` أو `controllers`.
- لا تُغيّر أو تحذف أي بيانات أصلية.
- استخدم اللغة العربية في التواصل معي أثناء التنفيذ.
- التزم بحرفية الخطوات أعلاه ولا تتجاوزها.
- مصدر البيانات الحصري:
  ```
  data/content/en
  data/content/fr
  ```

---

📘 **عند الانتهاء:**  
أرفق ملف `DATA_SEEDING_REPORT.md` في الجذر مع خلاصة التنفيذ.
