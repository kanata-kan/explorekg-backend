# 🚀 دليل الإعداد - Setup Guide

## 📋 المحتويات

- [المتطلبات](#-المتطلبات)
- [خطوات التثبيت](#-خطوات-التثبيت)
- [التشغيل](#-التشغيل)
- [التحقق](#-التحقق)

---

## 💻 المتطلبات

### البرامج المطلوبة

- **Node.js** v22.15.0 أو أحدث
- **MongoDB** v6.0 أو أحدث
- **pnpm** (أو npm/yarn)
- **Git**

### التحقق من الإصدارات

```bash
node --version   # v22.15.0
pnpm --version   # 9.x.x
mongod --version # 6.x.x
```

---

## 📦 خطوات التثبيت

### 1. استنساخ المشروع

```bash
git clone https://github.com/kanata-kan/explorekg-server.git
cd explorekg-server
```

### 2. تثبيت التبعيات

```bash
pnpm install
```

### 3. إعداد المتغيرات البيئية

```bash
cp .env.example .env
```

**تحرير `.env`**:

```env
PORT=4000
MONGODB_URI=mongodb://localhost:27017/explorekg
NODE_ENV=development
LOG_LEVEL=info
CORS_ORIGIN=*
```

### 4. تشغيل MongoDB

```bash
# Windows
net start MongoDB

# Linux/Mac
sudo systemctl start mongod
```

### 5. تشغيل الخادم

```bash
# Development mode
pnpm dev

# Production build
pnpm build
pnpm start
```

---

## ▶️ التشغيل

### Development Mode

```bash
pnpm dev
```

الخادم يعمل على: `http://localhost:4000`

### Production Mode

```bash
pnpm build
pnpm start
```

---

## ✅ التحقق

### 1. Health Check

```bash
curl http://localhost:4000/health
```

**Response**:

```json
{
  "status": "ok",
  "timestamp": "2025-11-02T10:30:00.000Z"
}
```

### 2. اختبار سريع

```bash
node --import tsx src/quick-test-guest.ts
```

---

## 🐛 حل المشاكل

### MongoDB Connection Error

```
Error: connect ECONNREFUSED 127.0.0.1:27017
```

**الحل**: تأكد من تشغيل MongoDB

```bash
sudo systemctl start mongod
```

### Port Already in Use

```
Error: listen EADDRINUSE: address already in use :::4000
```

**الحل**: غيّر PORT في `.env`

```env
PORT=5000
```

---

_📘 Auto-generated by Copilot Documentation Architect — ExploreKG Server Project_
